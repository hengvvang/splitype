[package]
name = "splitype-splitter"
version = "0.0.1"
edition = "2024"
license = "Apache-2.0"

[dependencies]
gpui = { version = "0.2.2" }

[dev-dependencies]
gpui = { version = "0.2.2", features = ["test-support"] }
