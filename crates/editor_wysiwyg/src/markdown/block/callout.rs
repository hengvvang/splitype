//! Callout kind — re-exported from `primitives`.
//!
//! The type lives in `primitives` (shared vocabulary between the Markdown
//! domain, the visual theme, and the editor renderers); the parser and
//! serializer refer to it through this re-export.

pub use primitives::CalloutKind;
