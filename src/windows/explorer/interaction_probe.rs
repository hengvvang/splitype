//! WIP: interactive explorer probes (local development only).
//!
//! This module is a stub so `#[cfg(test)] mod interaction_probe;` in
//! `mod.rs` compiles. Data-layer regression tests live in the modules they
//! exercise (see `worktree.rs` / `state.rs`); UI interaction probes can be
//! added here when they are ready.
